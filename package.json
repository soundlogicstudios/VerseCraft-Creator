{
  "name": "versecraft-creator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "VerseCraft Creator Phase 1: normalize + audit VerseCraft story JSONs (CLI core).",
  "scripts": {
    "validate": "node scripts/validate_story.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}